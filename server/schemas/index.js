// Importing type definitions from typeDefs.js file
const typeDefs = require("./typeDefs");
// Importing resolver functions from resolvers.js file
const resolvers = require("./resolvers");

// Exporting type definitions and resolver functions
module.exports = { typeDefs, resolvers };
